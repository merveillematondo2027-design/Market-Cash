import React,{useEffect,useState}from'react';
import{Building2,Code2,FileCheck2,IdCard,Layers3,MapPin,Store,UploadCloud,Clapperboard}from'lucide-react';
import{doc,onSnapshot}from'firebase/firestore';import{ref,uploadBytes}from'firebase/storage';import toast from'react-hot-toast';import{db,storage}from'../../firebase/config';import{useAuthStore}from'../../store/authStore';import{businessUpgradeService}from'../../services/businessUpgradeService';import{AccountUpgradeType}from'../../types';
const options=[{id:'marchand' as AccountUpgradeType,title:'Marchand / Business',text:'Encaissez les paiements Market-Cash dans votre entreprise.',icon:Building2},{id:'agent' as AccountUpgradeType,title:'Agent Market-Cash',text:'Devenez un point de service Market-Cash.',icon:Store},{id:'creator' as AccountUpgradeType,title:'Creator / Créateur de contenu',text:'Centralisez vos revenus numériques et préparez vos comptes internationaux partenaires.',icon:Clapperboard},{id:'developer_direct' as AccountUpgradeType,title:"Développeur d’application",text:'Intégrez Market-Cash dans vos propres applications.',icon:Code2},{id:'api_partner' as AccountUpgradeType,title:'Partenaire API',text:'Distribuez les services Market-Cash à vos développeurs.',icon:Layers3}];
const label=(t:AccountUpgradeType)=>options.find(x=>x.id===t)?.title||t;const safeName=(n:string)=>n.normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-zA-Z0-9._-]/g,'-').replace(/-+/g,'-').slice(-80)||'document';
export default function ProfessionalAccount(){const{user}=useAuthStore();const[type,setType]=useState<AccountUpgradeType>('marchand');const[companyName,setCompanyName]=useState('');const[address,setAddress]=useState('');const[identityFile,setIdentityFile]=useState<File|null>(null);const[companyFile,setCompanyFile]=useState<File|null>(null);const[status,setStatus]=useState('');const[reason,setReason]=useState('');const[busy,setBusy]=useState(false);useEffect(()=>{if(!user)return;return onSnapshot(doc(db,'account_upgrade_requests',user.uid),s=>{if(!s.exists()){setStatus('');return}const d=s.data();setStatus(String(d.status||'pending'));setReason(String(d.rejectionReason||''));if(d.requestedType)setType(d.requestedType);if(d.companyName)setCompanyName(String(d.companyName));if(d.address)setAddress(String(d.address))},()=>{})},[user?.uid]);if(!user)return null;
const submit=async(e:React.FormEvent)=>{e.preventDefault();if(!companyName.trim()||!address.trim())return toast.error(type==='creator'?"Indiquez votre nom Creator/professionnel et votre adresse.":"Indiquez le nom et l’adresse de l’entreprise.");if(!identityFile)return toast.error("Ajoutez votre pièce d’identité.");if(!companyFile)return toast.error(type==='creator'?"Ajoutez un justificatif d’activité/revenus en PDF.":"Ajoutez le document PDF de l’entreprise.");if(identityFile.size>12*1024*1024||companyFile.size>15*1024*1024)return toast.error('Document trop volumineux.');if(companyFile.type!=='application/pdf')return toast.error('Le document doit être au format PDF.');setBusy(true);try{const now=Date.now(),identityRef=ref(storage,`business-upgrades/${user.uid}/identity/${now}-${safeName(identityFile.name)}`),companyRef=ref(storage,`business-upgrades/${user.uid}/company/${now}-${safeName(companyFile.name.endsWith('.pdf')?companyFile.name:`${companyFile.name}.pdf`)}`);await uploadBytes(identityRef,identityFile,{contentType:identityFile.type});await uploadBytes(companyRef,companyFile,{contentType:'application/pdf'});await businessUpgradeService.submit({requestedType:type,companyName:companyName.trim(),address:address.trim(),identityDocumentPath:identityRef.fullPath,identityDocumentName:identityFile.name,companyDocumentPath:companyRef.fullPath,companyDocumentName:companyFile.name});setStatus('pending');toast.success(`Demande ${label(type)} envoyée.`)}catch(error:any){toast.error(error?.message||"Impossible d'envoyer la demande.")}finally{setBusy(false)}};
if(status==='pending')return <div className="mx-auto max-w-2xl p-4 pb-28"><div className="rounded-[2rem] border border-blue-200 bg-blue-50 p-6"><p className="text-xs font-black uppercase text-blue-700">Vérification professionnelle</p><h1 className="mt-2 text-2xl font-black">Demande {label(type)} en cours</h1><p className="mt-2 text-sm text-slate-600">Vos documents sont en cours de vérification.</p></div></div>;
return <div className="mx-auto max-w-3xl space-y-5 p-4 pb-28 md:p-8"><header><p className="text-sm font-semibold text-slate-500">Évolution du compte</p><h1 className="mt-1 text-3xl font-black">Passer au compte professionnel</h1></header>{status==='rejected'&&<div className="rounded-2xl border border-red-200 bg-red-50 p-4 text-sm text-red-700"><b>Demande précédente rejetée.</b> {reason}</div>}<section className="grid gap-3 sm:grid-cols-2">{options.map(o=>{const I=o.icon,active=type===o.id;return <button key={o.id} type="button" onClick={()=>setType(o.id)} className={`rounded-[1.75rem] border p-5 text-left ${active?'border-blue-950 bg-blue-50 ring-2 ring-blue-100':'bg-white'}`}><I className="text-blue-900"/><h2 className="mt-3 font-black">{o.title}</h2><p className="mt-2 text-xs leading-5 text-slate-500">{o.text}</p></button>})}</section><form onSubmit={submit} className="rounded-[2rem] border bg-white p-5 shadow-sm"><p className="text-xs font-black uppercase text-blue-700">{label(type)}</p><h2 className="mt-1 text-xl font-black">Documents de vérification</h2><div className="mt-4 space-y-3"><label className="block"><span className="mb-2 flex items-center gap-2 text-sm font-black"><Building2 size={16}/>{type==='creator'?'Nom Creator / nom professionnel':'Nom de l’entreprise'}</span><input value={companyName} onChange={e=>setCompanyName(e.target.value)} className="w-full rounded-2xl border p-4"/></label><label className="block"><span className="mb-2 flex items-center gap-2 text-sm font-black"><MapPin size={16}/>Adresse</span><input value={address} onChange={e=>setAddress(e.target.value)} className="w-full rounded-2xl border p-4"/></label><FileField icon={<IdCard size={18}/>} title="Pièce d’identité" accept="image/jpeg,image/png,image/webp,application/pdf" file={identityFile} onChange={setIdentityFile}/><FileField icon={<FileCheck2 size={18}/>} title={type==='creator'?"Justificatif d’activité / revenus":"Document de l’entreprise"} accept="application/pdf" file={companyFile} onChange={setCompanyFile}/></div><button disabled={busy} className="mt-5 flex w-full items-center justify-center gap-2 rounded-2xl bg-blue-950 p-4 font-black text-white"><UploadCloud size={19}/>{busy?'Envoi…':`Envoyer la demande ${label(type)}`}</button></form></div>}
function FileField({icon,title,accept,file,onChange}:{icon:React.ReactNode;title:string;accept:string;file:File|null;onChange:(f:File|null)=>void}){return <label className="block cursor-pointer rounded-2xl border border-dashed p-4"><span className="flex items-center gap-2 font-black">{icon}{title}</span><input type="file" accept={accept} onChange={e=>onChange(e.target.files?.[0]||null)} className="mt-3 block w-full text-sm"/>{file&&<span className="mt-2 block text-xs font-semibold text-emerald-700">{file.name}</span>}</label>}
