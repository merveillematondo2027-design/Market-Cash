import React,{useEffect,useMemo,useState}from'react';
import{ArrowLeft,CheckCircle2,Store}from'lucide-react';
import{Link,useSearchParams}from'react-router-dom';
import toast from'react-hot-toast';
import{agentWalletService,InternalCardSummary,MarketCashRecipient}from'../../services/agentWalletService';
import{WalletCurrency}from'../../types/wallet';
const key=()=>`pay_${Date.now()}_${Math.random().toString(36).slice(2,10)}`;const fmt=(v:number,c:WalletCurrency)=>c==='CDF'?`${Number(v||0).toLocaleString('fr-FR',{maximumFractionDigits:0})} CDF`:`${Number(v||0).toFixed(2)} USD`;
export default function MerchantPay(){const[params]=useSearchParams();const[currency,setCurrency]=useState<WalletCurrency>(params.get('currency')==='CDF'?'CDF':'USD');const[card,setCard]=useState<InternalCardSummary|null>(null);const[merchantId,setMerchantId]=useState((params.get('merchant')||params.get('id')||'').trim().toUpperCase());const[amount,setAmount]=useState((params.get('amount')||'').trim());const[merchant,setMerchant]=useState<MarketCashRecipient|null>(null);const[cvv,setCvv]=useState('');const[step,setStep]=useState<'form'|'review'|'cvv'|'done'>('form');const[busy,setBusy]=useState(false);const[reference,setReference]=useState('');const refresh=async()=>{const cards=await agentWalletService.getMyInternalCards();setCard(cards[0]||null)};useEffect(()=>{refresh().catch(()=>setCard(null))},[]);const balance=Number(card?.balances?.[currency]||0);const value=useMemo(()=>Number(String(amount).replace(',','.')),[amount]);const valid=Number.isFinite(value)&&value>0&&value<=balance;const identify=async()=>{if(!card||!merchantId.trim()||!valid)return;setBusy(true);try{setMerchant(await agentWalletService.lookupMerchantRecipient(merchantId.trim().toUpperCase()));setStep('review')}catch(e:any){toast.error(e?.message||'Marchand introuvable.')}finally{setBusy(false)}};const pay=async()=>{if(!card||!merchant||!valid||cvv.length!==3)return;setBusy(true);try{const r=await agentWalletService.payMerchant({cardId:card.cardId,marketCashId:merchant.marketCashId,currency,amount:value,cvv,idempotencyKey:key()});setReference(r.reference);setStep('done');setCvv('');await refresh()}catch(e:any){toast.error(e?.message||'Paiement refusé.')}finally{setBusy(false)}};
return <div className="mx-auto max-w-xl p-4 pb-28 md:p-8"><Link to="/client/cards?card=local" className="inline-flex items-center gap-2 text-sm font-bold text-slate-500"><ArrowLeft size={16}/>Ma carte locale</Link><section className="mt-5 rounded-[2rem] border bg-white p-5 shadow-sm md:p-6"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-amber-50 text-amber-700"><Store/></div><h1 className="mt-4 text-2xl font-black">Payer un marchand</h1><p className="mt-1 text-sm text-slate-500">Paiement depuis votre carte locale Market-Cash.</p>{!card?<div className="mt-5 rounded-2xl bg-slate-50 p-4 text-sm text-slate-500">Carte locale en cours de synchronisation…</div>:<>{step==='form'&&<div className="mt-5 space-y-3"><div className="grid grid-cols-2 gap-2 rounded-2xl bg-slate-100 p-1"><button onClick={()=>setCurrency('USD')} className={`rounded-xl py-3 font-black ${currency==='USD'?'bg-white shadow-sm':'text-slate-500'}`}>USD</button><button onClick={()=>setCurrency('CDF')} className={`rounded-xl py-3 font-black ${currency==='CDF'?'bg-white shadow-sm':'text-slate-500'}`}>CDF</button></div><label className="block text-xs font-black uppercase text-slate-500">ID marchand</label><input value={merchantId} onChange={e=>setMerchantId(e.target.value.toUpperCase())} placeholder="ID Market-Cash" className="w-full rounded-2xl border p-4 font-mono"/><label className="block text-xs font-black uppercase text-slate-500">Montant</label><input value={amount} onChange={e=>setAmount(e.target.value)} inputMode="decimal" placeholder={`Montant en ${currency}`} className="w-full rounded-2xl border p-4"/>{value>balance&&<p className="text-xs font-bold text-red-600">Solde insuffisant.</p>}<button disabled={busy||!merchantId.trim()||!valid} onClick={identify} className="w-full rounded-2xl bg-blue-950 py-4 font-black text-white disabled:opacity-40">{busy?'Vérification…':'Vérifier le marchand'}</button></div>}{step==='review'&&merchant&&<div className="mt-5 space-y-3"><div className="rounded-2xl bg-slate-50 p-4"><p className="text-xs text-slate-400">Marchand</p><p className="text-lg font-black">{merchant.displayName}</p><p className="mt-3 font-black">{fmt(value,currency)}</p></div><button onClick={()=>setStep('cvv')} className="w-full rounded-2xl bg-blue-950 py-4 font-black text-white">Confirmer</button></div>}{step==='cvv'&&<div className="mt-5 space-y-3"><label className="block text-xs font-black uppercase text-slate-500">CVV · 3 chiffres</label><input value={cvv} onChange={e=>setCvv(e.target.value.replace(/\D/g,'').slice(0,3))} type="password" inputMode="numeric" placeholder="•••" className="w-full rounded-2xl border-2 p-4 text-center text-2xl tracking-[.45em]"/><button disabled={busy||cvv.length!==3} onClick={pay} className="w-full rounded-2xl bg-emerald-600 py-4 font-black text-white disabled:opacity-40">{busy?'Paiement…':'Payer'}</button></div>}{step==='done'&&<div className="mt-6 rounded-3xl bg-emerald-50 p-6 text-center"><CheckCircle2 className="mx-auto text-emerald-600" size={42}/><h2 className="mt-3 text-xl font-black">Paiement envoyé</h2><p>{fmt(value,currency)} · {merchant?.displayName}</p><p className="mt-2 font-mono text-xs">{reference}</p></div>}</>}</section></div>}
