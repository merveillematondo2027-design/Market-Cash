import React,{useMemo}from'react';import{ArrowDownLeft,ArrowLeft,ArrowUpRight,History,Send}from'lucide-react';import{Link}from'react-router-dom';import{QRCodeSVG}from'qrcode.react';import toast from'react-hot-toast';import{useAuthStore}from'../../store/authStore';import{walletService}from'../../services/walletService';
type Action='send'|'receive'|'transactions'|'top-up';
export default function WalletAction({action}:{action:Action}){const{user}=useAuthStore();const wallet=useMemo(()=>user?.uid?walletService.getWalletPreview(user.uid):null,[user?.uid]);const meta={send:{title:'Envoyer',icon:Send},receive:{title:'Recevoir',icon:ArrowDownLeft},transactions:{title:'Transactions',icon:History},'top-up':{title:'Recharger',icon:ArrowUpRight}}[action];const Icon=meta.icon;const reserve=()=>toast('Cette opération sera activée via le backend Market-Cash et GMH APIs dès que le partenaire sera connecté.',{icon:'⏳'});return <div className="mx-auto max-w-xl p-4 md:p-8 pb-28"><Link to="/client/wallet" className="inline-flex items-center gap-2 text-sm font-bold text-slate-500"><ArrowLeft size={16}/> Retour au portefeuille</Link><section className="mt-5 rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-blue-50 text-blue-900"><Icon size={24}/></div><h1 className="mt-4 text-2xl font-black text-slate-950">{meta.title}</h1>{action==='receive'&&wallet?<div className="mt-6 text-center"><div className="inline-block rounded-3xl border border-slate-200 bg-white p-4"><QRCodeSVG value={`MARKET-CASH:WALLET:${wallet.localPaymentId}`} size={180}/></div><p className="mt-3 font-mono font-black text-blue-950">{wallet.localPaymentId}</p><p className="mt-1 text-sm text-slate-500">Présentez ce QR pour recevoir un paiement local Market-Cash.</p></div>:null}{action==='transactions'?<div className="mt-6 rounded-2xl bg-slate-50 p-7 text-center text-sm text-slate-500">Aucune transaction wallet pour le moment.</div>:null}{action==='send'?<div className="mt-6 space-y-3"><input disabled placeholder="Téléphone, ID Market-Cash ou QR" className="w-full rounded-2xl border border-slate-200 bg-slate-50 p-4 text-sm"/><input disabled placeholder="Montant" className="w-full rounded-2xl border border-slate-200 bg-slate-50 p-4 text-sm"/><button onClick={reserve} className="w-full rounded-2xl bg-blue-950 py-4 font-black text-white">Continuer</button></div>:null}{action==='top-up'?<div className="mt-6 space-y-3"><button onClick={reserve} className="w-full rounded-2xl border border-slate-200 p-4 text-left font-black text-slate-900">M-Pesa / Mobile Money<span className="mt-1 block text-xs font-medium text-slate-500">Demande sécurisée via GMH APIs</span></button><button onClick={reserve} className="w-full rounded-2xl border border-slate-200 p-4 text-left font-black text-slate-900">Agent / Terminal Market-Cash<span className="mt-1 block text-xs font-medium text-slate-500">Dépôt local sur le portefeuille</span></button></div>:null}</section></div>}
