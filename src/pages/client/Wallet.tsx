import React,{useEffect,useMemo,useState}from'react';import{Link,useSearchParams}from'react-router-dom';import{ArrowDownLeft,ArrowRightLeft,ArrowUpRight,Banknote,CreditCard,History,Send,ShieldCheck,ShoppingBag,WalletCards}from'lucide-react';import{useAuthStore}from'../../store/authStore';import{walletService}from'../../services/walletService';import{agentWalletService,WalletServerSnapshot}from'../../services/agentWalletService';import{WalletCurrency}from'../../types/wallet';
const money=(v:number,c:WalletCurrency)=>c==='CDF'?`${v.toLocaleString('fr-FR',{maximumFractionDigits:0})} CDF`:`${v.toFixed(2)} USD`;
export default function ClientWallet(){const{user}=useAuthStore();const[params]=useSearchParams();const initial=(params.get('currency')==='CDF'?'CDF':(localStorage.getItem('marketcash_wallet_currency')as WalletCurrency)||'USD')as WalletCurrency;const[currency,setCurrency]=useState<WalletCurrency>(initial);const[server,setServer]=useState<WalletServerSnapshot|null>(null);const[loading,setLoading]=useState(false);const preview=useMemo(()=>user?.uid?walletService.getWalletsPreview(user.uid):null,[user?.uid]);useEffect(()=>{if(!user?.uid)return;setLoading(true);agentWalletService.getMyWallets().then(setServer).catch(e=>console.warn('[WALLET_SERVER_UNAVAILABLE]',e)).finally(()=>setLoading(false));},[user?.uid]);const raw=server?.wallets?.[currency];const wallet=raw?{...preview?.[currency],...raw}:preview?.[currency];const transactions=walletService.getTransactionsPreview(currency);if(!wallet)return <div className="p-5">Portefeuille indisponible.</div>;const change=(c:WalletCurrency)=>{setCurrency(c);localStorage.setItem('marketcash_wallet_currency',c)};return <div className="mx-auto max-w-6xl p-4 md:p-8 pb-28 space-y-6"><header><p className="text-sm font-semibold text-slate-500">Votre portefeuille général</p><div className="flex items-center justify-between gap-3"><h1 className="mt-1 text-2xl md:text-3xl font-black text-slate-950">Market-Cash Wallet</h1>{loading&&<span className="text-[10px] font-bold text-slate-400">Mise à jour…</span>}</div><p className="mt-1 text-xs text-slate-500">Le portefeuille contient votre argent. Les cartes sont des moyens de paiement séparés qui pourront utiliser ce portefeuille.</p></header><section className="rounded-3xl bg-blue-950 p-6 text-white shadow-lg"><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-wider text-blue-200">Solde disponible</p><h2 className="mt-2 text-4xl font-black">{money(Number(wallet.availableBalance||0),currency)}</h2><p className="mt-3 flex items-center gap-2 text-xs font-semibold text-blue-200"><ShieldCheck size={14}/> Wallet {currency} actif</p></div><WalletCards size={30} className="text-amber-400"/></div><div className="mt-5 inline-flex rounded-2xl bg-white/10 p-1"><button onClick={()=>change('USD')} className={`px-5 py-2 rounded-xl text-sm font-black ${currency==='USD'?'bg-white text-blue-950':'text-blue-100'}`}>USD</button><button onClick={()=>change('CDF')} className={`px-5 py-2 rounded-xl text-sm font-black ${currency==='CDF'?'bg-white text-blue-950':'text-blue-100'}`}>CDF</button></div><div className="mt-5 grid grid-cols-2 gap-3 text-sm"><div className="rounded-2xl bg-white/10 p-4"><p className="text-xs text-blue-200">Solde comptable</p><p className="mt-1 font-black">{money(Number(wallet.ledgerBalance||0),currency)}</p></div><div className="rounded-2xl bg-white/10 p-4"><p className="text-xs text-blue-200">Montant réservé</p><p className="mt-1 font-black">{money(Number(wallet.heldBalance||0),currency)}</p></div></div>{server?.rechargeNumber&&<div className="mt-4 rounded-2xl bg-white/10 px-4 py-3"><p className="text-[10px] uppercase font-black text-blue-200">Mon numéro de recharge / retrait</p><p className="font-mono text-xl font-black tracking-wider">{server.rechargeNumber}</p><p className="text-[10px] text-blue-200 mt-1">À communiquer à un agent Market-Cash. Votre nom apparaîtra sur son terminal avant l’opération.</p></div>}</section><section className="grid grid-cols-3 md:grid-cols-7 gap-2">{[{n:'Envoyer',i:Send,to:'send'},{n:'Recevoir',i:ArrowDownLeft,to:'receive'},{n:'Payer',i:ShoppingBag,to:'pay'},{n:'Retirer',i:Banknote,to:'withdraw'},{n:'Recharger',i:ArrowUpRight,to:'top-up'},{n:'Convertir',i:ArrowRightLeft,to:'exchange'},{n:'Historique',i:History,to:'transactions'}].map(x=><Link key={x.n} to={`/client/wallet/${x.to}?currency=${currency}`} className="rounded-2xl border border-slate-200 bg-white px-2 py-4 text-center shadow-sm"><x.i className="mx-auto text-blue-800" size={21}/><span className="mt-2 block text-[11px] font-bold text-slate-700">{x.n}</span></Link>)}</section><Link to="/client/cards" className="flex items-center justify-between gap-3 rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><div><p className="text-[10px] font-black uppercase tracking-wider text-slate-400">Moyens de paiement</p><h2 className="mt-1 font-black text-slate-950">Cartes Market-Cash</h2><p className="mt-1 text-xs text-slate-500">Visa virtuelle et future carte physique Market-Cash restent gérées séparément du wallet.</p></div><CreditCard className="shrink-0 text-blue-800"/></Link><section className="rounded-3xl border bg-white shadow-sm"><div className="p-5 border-b flex justify-between"><h2 className="font-black">Activité {currency}</h2><span className="text-xs text-slate-400">Ledger serveur</span></div>{transactions.length===0&&<div className="p-8 text-center text-sm text-slate-500">Aucune transaction {currency} pour le moment.</div>}</section></div>}
