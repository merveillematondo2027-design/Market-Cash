import React,{useEffect,useState}from'react';
import{ArrowDownLeft,ArrowUpRight,History}from'lucide-react';
import{agentWalletService}from'../../services/agentWalletService';

export default function AgentHistory(){const[items,setItems]=useState<any[]>([]);const[loading,setLoading]=useState(true);useEffect(()=>{agentWalletService.getHistory().then(setItems).catch(()=>setItems([])).finally(()=>setLoading(false));},[]);return <div className="max-w-xl mx-auto p-4 md:p-8"><h1 className="text-2xl font-black text-blue-950">Historique agent</h1><p className="text-sm text-slate-500 mt-1">50 dernières opérations du terminal.</p><div className="mt-4 space-y-2">{loading?<div className="rounded-3xl bg-white border p-8 text-center text-slate-500">Chargement…</div>:items.length===0?<div className="rounded-3xl bg-white border p-8 text-center text-slate-500"><History className="mx-auto mb-2"/>Aucune transaction pour le moment.</div>:items.map(t=><div key={t.id} className="rounded-2xl bg-white border p-4 shadow-sm"><div className="flex items-start justify-between gap-3"><div className="flex gap-3"><div className={`w-10 h-10 rounded-xl grid place-items-center ${t.type==='cash_in'?'bg-emerald-50 text-emerald-700':'bg-amber-50 text-amber-700'}`}>{t.type==='cash_in'?<ArrowDownLeft size={18}/>:<ArrowUpRight size={18}/>}</div><div><p className="font-black text-slate-900">{t.type==='cash_in'?'Dépôt client':t.type==='cash_out'?'Retrait client':'Crédit agent'}</p><p className="text-[10px] font-mono text-slate-400">{t.reference}</p></div></div><div className="text-right"><p className="font-black text-blue-950">{Number(t.amount||0).toLocaleString('fr-FR')} {t.currency}</p><p className="text-[10px] text-slate-400">{new Date(t.createdAt).toLocaleString('fr-FR')}</p></div></div></div>)}</div></div>}
