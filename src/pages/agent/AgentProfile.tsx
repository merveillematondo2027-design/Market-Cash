import React,{useEffect,useState}from'react';
import{WalletCards}from'lucide-react';
import{useAuthStore}from'../../store/authStore';
import{agentWalletService,WalletServerSnapshot}from'../../services/agentWalletService';
export default function AgentProfile(){const{user}=useAuthStore();const[data,setData]=useState<WalletServerSnapshot|null>(null);useEffect(()=>{agentWalletService.ensureWalletProfile().then(setData).catch(()=>{});},[]);return <div className="max-w-xl mx-auto p-4 md:p-8"><h1 className="text-2xl font-black text-blue-950">Profil agent</h1><div className="mt-4 rounded-3xl bg-white border p-5 shadow-sm"><div className="w-12 h-12 rounded-2xl bg-blue-50 text-blue-800 grid place-items-center"><WalletCards/></div><h2 className="font-black text-lg mt-3">{user?.displayName||'Agent Market-Cash'}</h2><p className="text-sm text-slate-500">{user?.email}</p><p className="text-xs text-slate-400 mt-1">{user?.phone}</p>{data?.rechargeNumber&&<div className="mt-4 rounded-2xl bg-slate-50 p-4"><p className="text-[10px] font-black text-slate-400 uppercase">Numéro interne</p><p className="font-mono font-black text-blue-950">{data.rechargeNumber}</p></div>}<div className="mt-3 grid grid-cols-2 gap-2"><div className="rounded-2xl bg-slate-50 p-3"><p className="text-[10px] font-black text-slate-400">FLOAT CDF</p><p className="font-black">{Number(data?.wallets?.CDF?.availableBalance||0).toLocaleString('fr-FR')} CDF</p></div><div className="rounded-2xl bg-slate-50 p-3"><p className="text-[10px] font-black text-slate-400">FLOAT USD</p><p className="font-black">{Number(data?.wallets?.USD?.availableBalance||0).toFixed(2)} USD</p></div></div></div></div>}
