import React,{useEffect,useState}from'react';import{Banknote,History,User,WalletCards}from'lucide-react';import{Link}from'react-router-dom';import{agentWalletService}from'../../services/agentWalletService';
export default function AgentHome(){const[data,setData]=useState<any>(null);useEffect(()=>{agentWalletService.getMyAgentAccountSnapshot().then(setData).catch(()=>setData(null))},[]);return <div className="mx-auto max-w-3xl space-y-5 p-4 pb-28 md:p-8"><header><p className="text-xs font-black uppercase tracking-widest text-blue-700">Market-Cash Agent</p><h1 className="text-3xl font-black">Accueil</h1><p className="mt-2 text-sm text-slate-500">Votre activité Agent, vos soldes et vos raccourcis.</p></header><section className="grid grid-cols-2 gap-3">{['USD','CDF'].map(c=><div key={c} className="rounded-[2rem] border bg-white p-5"><WalletCards className="text-blue-800"/><p className="mt-4 text-xs font-black text-slate-400">SOLDE {c}</p><p className="mt-1 text-2xl font-black">{Number(data?.wallets?.[c]?.availableBalance||0).toLocaleString('fr-FR')} {c}</p></div>)}</section><section className="grid grid-cols-2 gap-3"><Link to="/agent/terminal" className="rounded-[2rem] border bg-white p-5"><Banknote className="text-blue-800"/><b className="mt-4 block">Terminal</b><span className="text-xs text-slate-500">Dépôts et retraits clients</span></Link><Link to="/agent/history" className="rounded-[2rem] border bg-white p-5"><History className="text-blue-800"/><b className="mt-4 block">Transactions</b><span className="text-xs text-slate-500">Historique des opérations</span></Link><Link to="/agent/profile" className="rounded-[2rem] border bg-white p-5"><User className="text-blue-800"/><b className="mt-4 block">Profil</b><span className="text-xs text-slate-500">Identité et paramètres</span></Link></section></div>}
